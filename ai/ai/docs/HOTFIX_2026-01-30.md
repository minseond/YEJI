# AI 서버 핫픽스 작업 기록 (2026-01-30)

## 개요

`ai/main` 브랜치에서 `/api/v1/fortune/chat/stream` 엔드포인트 호출 시 발생하는 연쇄적인 AttributeError 수정 작업.

**근본 원인**: `ai/main`의 서비스 코드(tikitaka_service.py, llm_interpreter.py)가 기대하는 스키마와 `user_fortune.py`의 Pydantic 모델 스키마 불일치.

---

## 핫픽스 목록

### Hotfix #1: Pillar 모델 필드 누락 (MR #28)

**브랜치**: `ai/hotfix/pillar-gan-code`

**에러 메시지**:
```
'Pillar' object has no attribute 'gan_code'
```

**원인**:
- `llm_interpreter.py:581`에서 `chart.year.gan_code.hangul` 접근
- `user_fortune.py`의 `Pillar` 클래스에 `gan_code`, `ji_code`, `ten_god_code` 필드 누락

**수정 파일**:
1. `ai/src/yeji_ai/models/user_fortune.py`
   - `CheonGanCode`, `JiJiCode`, `TenGodCode` import 추가
   - `Pillar` 클래스에 필드 추가:
     ```python
     gan_code: CheonGanCode = Field(..., description="천간 코드")
     ji_code: JiJiCode = Field(..., description="지지 코드")
     ten_god_code: TenGodCode = Field(..., description="십신 코드")
     ```

2. `ai/src/yeji_ai/services/eastern_fortune_service.py`
   - `pillar_to_model()` 메서드에서 새 필드 채우기:
     ```python
     gan_code = STEM_TO_CODE[stem]
     ji_code = BRANCH_TO_CODE[branch]
     ten_god_code = self.get_ten_god(day_stem, stem)  # 일주는 DAY_MASTER
     ```

**커밋**: `fix: [AI] Pillar 모델에 gan_code, ji_code, ten_god_code 필드 추가`

---

### Hotfix #2: element_code 타입 불일치 (MR #29)

**브랜치**: `ai/hotfix/pillar-element-code`

**에러 메시지**:
```
'str' object has no attribute 'label_ko'
```

**원인**:
- `llm_interpreter.py:581`에서 `chart.day.element_code.label_ko` 접근
- `Pillar.element_code`가 문자열(`"WOOD"`)인데 `ElementCode` Enum으로 기대

**수정 파일**:
1. `ai/src/yeji_ai/models/user_fortune.py`
   - `ElementCode` import 추가
   - `element_code: EastElementLiteral` → `element_code: ElementCode`

2. `ai/src/yeji_ai/services/eastern_fortune_service.py`
   - `element_code=element.value` → `element_code=element`
   - 폴백 케이스도 `element_code=ElementCode.EARTH`로 수정

**커밋**: `fix: [AI] Pillar.element_code를 ElementCode Enum으로 변경`

---

### Hotfix #3: FiveElements dict 호환성 (MR #30)

**브랜치**: `ai/hotfix/five-elements-get`

**에러 메시지**:
```
'FiveElements' object has no attribute 'get'
```

**원인**:
- `tikitaka_service.py:251`에서 `stats.five_elements.get('dominant', '균형')` 호출
- `FiveElements`는 Pydantic 모델이라 `.get()` 메서드 없음

**수정 파일**:
1. `ai/src/yeji_ai/models/user_fortune.py`

   **FiveElements 클래스에 추가**:
   ```python
   @property
   def dominant(self) -> str:
       """우세 오행 레이블 반환"""
       if not self.elements_list:
           return "균형"
       max_elem = max(self.elements_list, key=lambda x: x.percent)
       return max_elem.label

   @property
   def strong(self) -> str:
       """강한 오행 레이블 반환 (dominant와 동일)"""
       return self.dominant

   @property
   def weak(self) -> str:
       """약한 오행 레이블 반환"""
       if not self.elements_list:
           return "균형"
       min_elem = min(self.elements_list, key=lambda x: x.percent)
       return min_elem.label

   def get(self, key: str, default: str = "") -> str:
       """dict 호환용 get 메서드"""
       if key == "dominant":
           return self.dominant
       if key == "strong":
           return self.strong
       if key == "weak":
           return self.weak
       if key == "summary":
           return self.summary
       return default
   ```

   **EasternStats 클래스에 추가**:
   ```python
   @property
   def yin_yang(self) -> YinYangRatio:
       """yin_yang_ratio의 별칭 (하위 호환용)"""
       return self.yin_yang_ratio

   @property
   def strength(self) -> str:
       """강한 오행 정보 (five_elements.dominant의 별칭)"""
       return self.five_elements.dominant
   ```

**커밋**: `fix: [AI] FiveElements/EasternStats에 dict 호환 속성 추가`

---

## 미해결 문제

### 현재 에러
```
'EasternStats' object has no attribute 'weakness'
```

**원인**: 서비스 코드에서 `stats.weakness` 접근하는데, `EasternStats`에 `weakness` 속성 없음.

**해결 방안 (선택 필요)**:
1. **계속 핫픽스**: `EasternStats`에 `weakness` property 추가 (추가 에러 발생 가능)
2. **develop 전체 머지**: `ai/develop` → `ai/main` 머지 (develop 검증 필요)
3. **서비스 코드 대폭 수정**: `tikitaka_service.py`, `llm_interpreter.py` 리팩토링

---

## Jenkins 빌드 이력

| 빌드 | 커밋 | 결과 |
|------|------|------|
| #16 | fix: [Infra] VLLM_BASE_URL에서 /v1 제거 | SUCCESS |
| #17 | Merge ai/hotfix/pillar-gan-code | SUCCESS |
| #18 | Merge ai/hotfix/pillar-element-code | SUCCESS |
| #19 | Merge ai/hotfix/five-elements-get | SUCCESS |

---

## 테스트 명령어

```bash
# API 테스트 (SSE 스트리밍)
curl -N -X POST "https://i14a605.p.ssafy.io/ai/api/v1/fortune/chat/stream" \
  -H "Content-Type: application/json" \
  -d '{"message": "오늘 운세 알려줘", "birth_date": "1995-03-15", "birth_time": "14:30"}'

# 로컬 테스트
cd C:/Users/SSAFY/yeji-ai-server/ai
uv run python -c "
from yeji_ai.services.eastern_fortune_service import EasternFortuneService
svc = EasternFortuneService()
pillar = svc.pillar_to_model('갑자', '갑')
print(f'gan_code.hangul: {pillar.gan_code.hangul}')
print(f'element_code.label_ko: {pillar.element_code.label_ko}')
"
```

---

## 권장 사항

> **ai/main과 ai/develop 스키마 불일치가 근본 원인입니다.**
>
> 핫픽스로 계속 땜질하는 것보다 `ai/develop`의 변경사항을 검증 후 `ai/main`으로 머지하는 것이 장기적으로 바람직합니다.

---

*작성일: 2026-01-30*
*작성자: Claude Code (Hotfix Session)*
